import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#dd2c00",
        },
        secondary: {
            main: "#dd0042",
        }
    },
    typography: {
        fontFamily: [
            'Inter'
        ]
    }
});