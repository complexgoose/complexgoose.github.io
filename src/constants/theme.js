import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#f4511e",
        },
        secondary: {
            main: "#5e35b1",
        }
    },
    typography: {
        fontFamily: [
            'Inter'
        ]
    }
});